package org.foocorp.exercise.contactlist.model;

/**
 * Simple enum; here we'll use lowercase values to be lazy, and let Jackson render them as expected, by default
 */
public enum PhoneType {
  home, work, mobile
}
